{
  "name": "mindful-diary-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && node app.js\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
